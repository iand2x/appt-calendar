# API Configuration
# Set to 'mock' for mock API or 'graphql' for GraphQL API
VITE_API_TYPE=mock

# GraphQL endpoint (only needed when VITE_API_TYPE=graphql)
VITE_GRAPHQL_ENDPOINT=http://localhost:3000/graphql

# For production GraphQL endpoint
# VITE_GRAPHQL_ENDPOINT=https://your-lambda-endpoint.amazonaws.com/graphql
